using System.ComponentModel.Design;

namespace Krypton.Toolkit.Suite.Extended.Circular.Progress.Bar
{
    /*
    internal class CircularProgressBarActionList : DesignerActionList
    {
        #region Instance Fields
        private readonly CircularProgressBar _circularProgressBar;

        private readonly IComponentChangeService _service;
        #endregion

        #region Identity
        public CircularProgressBarActionList(CircularProgressBar owner) : base(owner.Component)
        {
            _circularProgressBar = owner.Component as CircularProgressBar;

            _service = (IComponentChangeService)GetService(typeof(IComponentChangeService));
        }
        #endregion

        #region Public Override
        public override DesignerActionItemCollection GetSortedActionItems()
        {
            return base.GetSortedActionItems();
        }
        #endregion
    }
    */
}